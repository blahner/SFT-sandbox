[project]
name = "SFT-sandbox"
version = "0.1.0"
description = "Sandbox environment for basic supervised finetuning of an LLM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.117.1",
    "datasets>=5.0.0",
    "peft>=0.19.1",
    "torch>=2.13.0",
    "transformers>=5.14.1",
    "trl>=1.9.0",
]
